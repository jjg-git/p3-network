package group3.p3network;

public class Commands {
    private static Command.Builder builder = Command.newBuilder();

    public static Command ListVideos = builder.setCommand("List Videos").build();
}
